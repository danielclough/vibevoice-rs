# VibeVoice Server Configuration

# Server binding
host: 0.0.0.0
port: 3000

# Directory containing voice safetensors files (for realtime model)
# Voice names in API requests will be resolved from this directory
# e.g. "en-Emma_woman" -> "/path/to/voices/en-Emma_woman.safetensors"
voices_dir: /Users/daniel/git/vibevoice-rs/voices/streaming_model

# Directory containing WAV samples for voice cloning (for batch models)
samples_dir: /Users/daniel/git/vibevoice-rs/voices/

# Optional directory to save output WAV files
output_dir: /Users/daniel/git/vibevoice-rs/output/

# Directory containing built vibevoice-web frontend to serve at /
web_dir: /Users/daniel/git/vibevoice-rs/vibevoice-web/dist
